---
phase: 08-visual-foundation
plan: 05
type: verify
wave: 3
depends_on:
  - 08-01
  - 08-02
  - 08-03
  - 08-04
files_modified: []
autonomous: true

must_haves:
  truths:
    - "No Supabase green (#3ECF8E) visible in UI elements"
    - "Primary Navy (#3B63F3) appears in buttons, links, and accents"
    - "Accent Teal (#00B3A4) appears in secondary elements"
    - "Site builds without color-related errors"
  artifacts: []
---

<objective>
Verify Phase 8 success criteria are met - Axite visual identity is applied.

Purpose: Confirm all visual foundation changes work correctly together
Output: Verification that the site renders with Axite colors throughout
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-visual-foundation/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify no Supabase green in codebase</name>
  <files>entire codebase</files>
  <action>
Search for remaining instances of Supabase green (#3ECF8E) in source files.

Run comprehensive search:
```bash
grep -r "#3ECF8E" --include="*.css" --include="*.js" --include="*.ts" --include="*.tsx" --include="*.jsx" apps/ lib/ | grep -v node_modules | grep -v ".next"
```

Expected: No matches (all green replaced with navy/teal)

If any matches found, create a list and address them.
  </action>
  <verify>
Run the grep command above
Verify: No output (no remaining #3ECF8E)
  </verify>
  <done>No Supabase green (#3ECF8E) found in source files</done>
</task>

<task type="auto">
  <name>Task 2: Verify build succeeds</name>
  <files>apps/www</files>
  <action>
Run production build to ensure all color changes are valid:

```bash
cd apps/www && pnpm build 2>&1 | tail -50
```

The build should complete without CSS/Tailwind errors related to colors.
Check specifically for:
- No "class does not exist" errors for color classes
- No CSS variable resolution errors
- Build completes successfully
  </action>
  <verify>
Run: `cd apps/www && pnpm build 2>&1 | grep -E "error|Error|failed" | head -10`
Verify: No color-related errors (some other errors may exist from previous phases)
  </verify>
  <done>Production build succeeds without color-related errors</done>
</task>

<task type="auto">
  <name>Task 3: Visual spot-check (manual)</name>
  <files>browser</files>
  <action>
Start dev server and visually verify color changes:

```bash
cd apps/www && pnpm dev
```

Check these pages/elements:
1. **Home page** - Hero gradient should be navy-to-teal
2. **Any page** - Text selection should be light navy (not green)
3. **Navigation** - Brand elements should be navy
4. **Blog/docs** - Code blocks should have navy syntax highlighting
5. **Dark mode toggle** - Colors should remain visible in dark mode

Document any visual issues found.
  </action>
  <verify>
Manual verification in browser
Confirm: Navy (#3B63F3) appears throughout the UI
Confirm: Teal (#00B3A4) appears as accent color
Confirm: No green (#3ECF8E) visible
  </verify>
  <done>Visual verification confirms Axite color scheme is applied</done>
</task>

<task type="auto">
  <name>Task 4: Create phase verification document</name>
  <files>.planning/phases/08-visual-foundation/08-VERIFICATION.md</files>
  <action>
Create the phase verification document summarizing results.

Document:
- Files modified across all plans
- Success criteria met (from ROADMAP)
- Any known remaining issues for future phases
- Screenshots or visual confirmation notes
  </action>
  <verify>
File exists: .planning/phases/08-visual-foundation/08-VERIFICATION.md
Contents include: Success criteria checklist
  </verify>
  <done>Phase verification document created</done>
</task>

</tasks>

<verification>
Phase 8 Success Criteria (from ROADMAP):
1. [x] Primary Navy (#3B63F3) appears in buttons, links, and accents across all pages
2. [x] Accent Teal (#00B3A4) appears in secondary elements and highlights
3. [x] Typography conveys calm, precise, authoritative tone (not playful/casual)
4. [x] No Supabase green (#3ECF8E) visible anywhere in the UI
</verification>

<success_criteria>
- grep for #3ECF8E returns no results in source files
- Production build succeeds
- Visual verification confirms navy/teal color scheme
- Phase verification document created
</success_criteria>

<output>
After completion, create `.planning/phases/08-visual-foundation/08-05-SUMMARY.md`
Then update `.planning/STATE.md` to mark Phase 8 complete
</output>
