---
phase: 08-visual-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - apps/www/data/CodeEditorTheme.js
  - apps/www/components/CodeBlock/CodeBlock.utils.js
  - lib/ui/src/components/CodeBlock/CodeBlock.utils.ts
  - apps/www/components/Products/Functions/Metrics.tsx
autonomous: true

must_haves:
  truths:
    - "Code editor syntax highlighting uses navy (#3B63F3) instead of green (#3ECF8E)"
    - "Code block utils in both apps/www and lib/ui use navy"
    - "Metrics component color constant uses brand CSS variable"
  artifacts:
    - path: "apps/www/data/CodeEditorTheme.js"
      provides: "Code editor theme colors"
      contains: "#3B63F3"
    - path: "apps/www/components/CodeBlock/CodeBlock.utils.js"
      provides: "Code block syntax colors"
      contains: "#3B63F3"
    - path: "lib/ui/src/components/CodeBlock/CodeBlock.utils.ts"
      provides: "Shared code block syntax colors"
      contains: "#3B63F3"
---

<objective>
Update code editor and syntax highlighting colors from Supabase green to Axite navy.

Purpose: Ensure code blocks and editors throughout the site use brand colors
Output: All code-related components use navy (#3B63F3) for highlighting
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-visual-foundation/08-CONTEXT.md
@apps/www/data/CodeEditorTheme.js
@apps/www/components/CodeBlock/CodeBlock.utils.js
@lib/ui/src/components/CodeBlock/CodeBlock.utils.ts
@apps/www/components/Products/Functions/Metrics.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CodeEditorTheme.js</name>
  <files>apps/www/data/CodeEditorTheme.js</files>
  <action>
Replace all instances of `#3ECF8E` with `#3B63F3` in apps/www/data/CodeEditorTheme.js.

This file contains the Prism/Monaco editor theme colors for code syntax highlighting.
The green color is used for various syntax elements like:
- comments
- strings
- keywords
- functions
- operators

Use find/replace:
- Find: `#3ECF8E`
- Replace: `#3B63F3`

This should update approximately 15 color references.
  </action>
  <verify>
Run: `grep -c "#3B63F3" apps/www/data/CodeEditorTheme.js`
Verify: Count is approximately 15 (all occurrences replaced)
Run: `grep "#3ECF8E" apps/www/data/CodeEditorTheme.js`
Verify: No matches found (all green removed)
  </verify>
  <done>CodeEditorTheme.js uses navy for all syntax highlighting</done>
</task>

<task type="auto">
  <name>Task 2: Update CodeBlock.utils.js in apps/www</name>
  <files>apps/www/components/CodeBlock/CodeBlock.utils.js</files>
  <action>
Replace all instances of `#3ECF8E` with `#3B63F3` in apps/www/components/CodeBlock/CodeBlock.utils.js.

This file provides syntax highlighting colors for code blocks in the www app.

Use find/replace:
- Find: `#3ECF8E`
- Replace: `#3B63F3`

This should update approximately 15 color references.
  </action>
  <verify>
Run: `grep -c "#3B63F3" apps/www/components/CodeBlock/CodeBlock.utils.js`
Verify: Count is approximately 15
Run: `grep "#3ECF8E" apps/www/components/CodeBlock/CodeBlock.utils.js`
Verify: No matches found
  </verify>
  <done>apps/www CodeBlock.utils.js uses navy syntax colors</done>
</task>

<task type="auto">
  <name>Task 3: Update CodeBlock.utils.ts in lib/ui</name>
  <files>lib/ui/src/components/CodeBlock/CodeBlock.utils.ts</files>
  <action>
Replace all instances of `#3ECF8E` with `#3B63F3` in lib/ui/src/components/CodeBlock/CodeBlock.utils.ts.

This is the shared/library version of the code block utilities.

Use find/replace:
- Find: `#3ECF8E`
- Replace: `#3B63F3`

This should update approximately 14 color references.
  </action>
  <verify>
Run: `grep -c "#3B63F3" lib/ui/src/components/CodeBlock/CodeBlock.utils.ts`
Verify: Count is approximately 14
Run: `grep "#3ECF8E" lib/ui/src/components/CodeBlock/CodeBlock.utils.ts`
Verify: No matches found
  </verify>
  <done>lib/ui CodeBlock.utils.ts uses navy syntax colors</done>
</task>

<task type="auto">
  <name>Task 4: Update Metrics.tsx colors</name>
  <files>apps/www/components/Products/Functions/Metrics.tsx</files>
  <action>
Update the color constant in apps/www/components/Products/Functions/Metrics.tsx.

Find the COLORS or color constant definition (around line 9):
```javascript
GREEN_1: 'hsl(var(--brand-default))', // #3ECF8E
```

Update the comment to reflect the new color:
```javascript
GREEN_1: 'hsl(var(--brand-default))', // #3B63F3 Navy
```

Note: The actual color value uses a CSS variable which will automatically pick up the new brand color from Plan 01. We just need to update the comment.

If there are any hardcoded hex values, replace them:
- Find: `#3ECF8E`
- Replace: `#3B63F3`
  </action>
  <verify>
Run: `grep -E "#3ECF8E|#3B63F3|brand-default" apps/www/components/Products/Functions/Metrics.tsx`
Verify: No #3ECF8E found, brand-default or #3B63F3 is present
  </verify>
  <done>Metrics.tsx uses brand CSS variable (inherits navy)</done>
</task>

</tasks>

<verification>
1. Run `grep -r "#3ECF8E" apps/www/data/CodeEditorTheme.js apps/www/components/CodeBlock/ lib/ui/src/components/CodeBlock/`
   - Should return no matches
2. Run the dev server and view any page with code blocks
   - Syntax highlighting should use navy tones instead of green
</verification>

<success_criteria>
- No #3ECF8E in CodeEditorTheme.js
- No #3ECF8E in either CodeBlock.utils file
- Metrics component uses CSS variable (inherits brand color)
- Code blocks render with navy syntax highlighting
</success_criteria>

<output>
After completion, create `.planning/phases/08-visual-foundation/08-03-SUMMARY.md`
</output>
