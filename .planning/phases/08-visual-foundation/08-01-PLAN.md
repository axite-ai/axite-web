---
phase: 08-visual-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/ui/src/styles/variables.css
  - lib/config/default-colors.js
autonomous: true

must_haves:
  truths:
    - "Primary Navy (#3B63F3) is defined as brand-default CSS variable"
    - "Accent Teal (#00B3A4) is available as a secondary brand color"
    - "Dark mode brand colors use appropriate navy/teal variants"
  artifacts:
    - path: "lib/ui/src/styles/variables.css"
      provides: "CSS custom properties for Axite brand colors"
      contains: "--brand-default: 224 90% 59%"
    - path: "lib/config/default-colors.js"
      provides: "Brand color scales for Tailwind"
      contains: "hsla(224"
  key_links:
    - from: "lib/config/tailwind.config.js"
      to: "lib/ui/src/styles/variables.css"
      via: "CSS variable references"
      pattern: "var(--brand"
---

<objective>
Define Axite brand colors in CSS variables and Tailwind color scales.

Purpose: Establish the color foundation that all UI components will inherit
Output: CSS variables and JS color scales using Navy (#3B63F3) and Teal (#00B3A4)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-visual-foundation/08-CONTEXT.md
@lib/ui/src/styles/variables.css
@lib/config/default-colors.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CSS variables with Axite brand colors</name>
  <files>lib/ui/src/styles/variables.css</files>
  <action>
Update the CSS custom properties in variables.css to use Axite brand colors:

**Primary Navy (#3B63F3) - HSL: 224 90% 59%**
**Accent Teal (#00B3A4) - HSL: 174 100% 35%**

Replace the brand color section in `:root`:
```css
/* Brand colors - Axite Navy and Teal */
--brand-default: 224 90% 59%;      /* Primary Navy #3B63F3 */
--brand-300: 224 85% 70%;          /* Lighter Navy */
--brand-500: 224 95% 50%;          /* Darker Navy */
--brand-accent: 174 100% 35%;      /* Accent Teal #00B3A4 */
--brand-accent-light: 174 80% 50%; /* Lighter Teal */
```

Update the `--background-selection` to use a navy tint instead of green:
```css
--background-selection: 224 60% 90%;
```

For dark mode (`.dark, [data-theme*="dark"]`):
```css
/* Brand colors stay vibrant in dark mode */
--brand-default: 224 90% 65%;      /* Slightly lighter Navy for dark bg */
--brand-300: 224 80% 75%;
--brand-500: 224 95% 55%;
--brand-accent: 174 100% 45%;      /* Slightly lighter Teal */
--brand-accent-light: 174 80% 55%;

/* Selection with navy tint */
--background-selection: 224 40% 25%;
```

Also update `--colors-green12` to use navy for any gradient references:
```css
--colors-green12: #3B63F3;  /* Replace green with navy */
```
  </action>
  <verify>
Run: `grep -E "brand-default|brand-accent|224.*90%|174.*100%" lib/ui/src/styles/variables.css`
Verify: Navy and teal HSL values appear for brand variables
  </verify>
  <done>CSS variables define Axite navy as brand-default and teal as brand-accent</done>
</task>

<task type="auto">
  <name>Task 2: Update brand color scales in default-colors.js</name>
  <files>lib/config/default-colors.js</files>
  <action>
Update the brand color scale in default-colors.js to use Navy-based colors.

The scale goes from 1 (lightest) to 12 (darkest).

Replace the `brand` object with Navy-based scale:
```javascript
brand: {
  brand1: 'hsla(224, 100%, 99%, 1)',   // Nearly white with navy tint
  brand2: 'hsla(224, 90%, 97%, 1)',    // Very light navy
  brand3: 'hsla(224, 85%, 95%, 1)',
  brand4: 'hsla(224, 80%, 92%, 1)',
  brand5: 'hsla(224, 75%, 88%, 1)',
  brand6: 'hsla(224, 70%, 82%, 1)',
  brand7: 'hsla(224, 75%, 72%, 1)',
  brand8: 'hsla(224, 85%, 62%, 1)',
  brand9: 'hsla(224, 90%, 59%, 1)',    // Primary Navy #3B63F3
  brand10: 'hsla(224, 85%, 55%, 1)',
  brand11: 'hsla(224, 80%, 45%, 1)',
  brand12: 'hsla(224, 70%, 20%, 1)',   // Very dark navy
},
```

Replace the `brandDark` object for dark mode:
```javascript
brandDark: {
  brand1: 'hsla(224, 80%, 8%, 1)',     // Very dark navy bg
  brand2: 'hsla(224, 75%, 10%, 1)',
  brand3: 'hsla(224, 70%, 13%, 1)',
  brand4: 'hsla(224, 68%, 16%, 1)',
  brand5: 'hsla(224, 66%, 19%, 1)',
  brand6: 'hsla(224, 64%, 24%, 1)',
  brand7: 'hsla(224, 70%, 32%, 1)',
  brand8: 'hsla(224, 80%, 42%, 1)',
  brand9: 'hsla(224, 90%, 59%, 1)',    // Primary Navy (same)
  brand10: 'hsla(224, 90%, 70%, 1)',   // Lighter for dark mode visibility
  brand11: 'hsla(224, 85%, 60%, 1)',
  brand12: 'hsla(224, 80%, 95%, 1)',   // Near white for text on dark
},
```
  </action>
  <verify>
Run: `grep -c "hsla(224" lib/config/default-colors.js`
Verify: Multiple lines contain the navy hue (224)
Run: `grep "brand9:" lib/config/default-colors.js`
Verify: brand9 shows the primary navy color (224, 90%, 59%)
  </verify>
  <done>Brand color scales use navy-based HSL values for both light and dark modes</done>
</task>

</tasks>

<verification>
1. Run the dev server: `cd apps/www && pnpm dev`
2. Check any page - brand colored elements should appear navy instead of green
3. Toggle dark mode - colors should remain visible and appropriately adjusted
4. Verify no build errors: `cd apps/www && pnpm build 2>&1 | head -50`
</verification>

<success_criteria>
- CSS variables define navy (#3B63F3) as brand-default
- Teal (#00B3A4) available as brand-accent
- Color scales provide full 1-12 range in navy
- Dark mode has appropriate lighter variants
</success_criteria>

<output>
After completion, create `.planning/phases/08-visual-foundation/08-01-SUMMARY.md`
</output>
