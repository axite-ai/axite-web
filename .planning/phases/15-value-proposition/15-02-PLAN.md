---
phase: 15-value-proposition
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/www/components/PillarsSection/PolicyPillar.tsx
  - apps/www/components/PillarsSection/IdentityPillar.tsx
  - apps/www/components/PillarsSection/AuditPillar.tsx
  - apps/www/components/PillarsSection/index.tsx
autonomous: true

must_haves:
  truths:
    - "Enforceable Policy pillar visible with mini policy demo"
    - "Identity/Least Privilege pillar visible with permission matrix visual"
    - "Audit Evidence pillar visible with report preview visual"
    - "Policy pillar has more visual weight than Identity and Audit"
  artifacts:
    - path: "apps/www/components/PillarsSection/PolicyPillar.tsx"
      provides: "Featured policy pillar component"
      exports: ["default"]
    - path: "apps/www/components/PillarsSection/IdentityPillar.tsx"
      provides: "Identity pillar component"
      exports: ["default"]
    - path: "apps/www/components/PillarsSection/AuditPillar.tsx"
      provides: "Audit pillar component"
      exports: ["default"]
    - path: "apps/www/components/PillarsSection/index.tsx"
      provides: "Main pillars section"
      exports: ["default"]
  key_links:
    - from: "PillarsSection/index.tsx"
      to: "PolicyPillar, IdentityPillar, AuditPillar"
      via: "imports and renders"
      pattern: "import.*Pillar"
---

<objective>
Create PillarsSection with three pillar components (Policy featured, Identity, Audit)

Purpose: Explain HOW Axite delivers the transformation through its three core mechanisms

Output: PillarsSection with visual components for each pillar
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-value-proposition/15-CONTEXT.md

Reference existing components:
@apps/www/components/ProofSection/PolicyDemo.tsx (policy demo pattern - adapt for mini version)
@apps/www/components/Panel/Panel.tsx (card styling)
@apps/www/components/Layouts/SectionContainer.tsx (section wrapper)
@apps/www/components/UI/SectionHeader.tsx (section header)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PolicyPillar component (featured)</name>
  <files>apps/www/components/PillarsSection/PolicyPillar.tsx</files>
  <action>
Create FEATURED PolicyPillar component - larger than other pillars.

Visual: Mini policy demo showing allow/deny (simplified version of PolicyDemo from ProofSection):
- Single Panel with window dots header
- Show 2-3 policy decision lines (one allowed, one blocked)
- Use Badge component for result indicators
- Compact format: timestamp | action | result badge

Content:
- Title: "Enforceable Policy"
- Description: "Define what agents can and cannot do. Policies evaluate every tool call before execution."
- Visual shows real decisions in action

Layout: Takes more horizontal space (col-span-full or col-span-8 on desktop)

Use existing patterns from PolicyDemo.tsx but more compact.
  </action>
  <verify>
File exists and exports default component
  </verify>
  <done>
PolicyPillar.tsx exists with mini policy demo visual showing allow/deny decisions
  </done>
</task>

<task type="auto">
  <name>Task 2: Create IdentityPillar component</name>
  <files>apps/www/components/PillarsSection/IdentityPillar.tsx</files>
  <action>
Create IdentityPillar component - compact size.

Visual: Permission matrix (simple grid showing agent/tool/permission combinations):
- 3-4 rows (agents): support-bot, data-agent, admin-agent
- 3-4 columns (permissions): read, write, delete, admin
- Use checkmarks/X marks or colored cells to show permissions
- Use Panel component for card styling

Content:
- Title: "Identity & Least Privilege"
- Description: "Every agent has an identity. Every identity has explicit permissions. No implicit access."

Layout: Smaller than Policy pillar (col-span-6 or col-span-4 on desktop)

Keep it simple - static table/grid, no animations needed.
  </action>
  <verify>
File exists and exports default component
  </verify>
  <done>
IdentityPillar.tsx exists with permission matrix visual
  </done>
</task>

<task type="auto">
  <name>Task 3: Create AuditPillar component</name>
  <files>apps/www/components/PillarsSection/AuditPillar.tsx</files>
  <action>
Create AuditPillar component - compact size.

Visual: Report/dashboard preview (mock audit report):
- Panel component with "Audit Report" header
- Show summary stats: total actions, allowed, blocked, unique agents
- Mini bar or percentage indicators
- Time range indicator (e.g., "Last 7 days")

Content:
- Title: "Audit-Grade Evidence"
- Description: "Complete decision traces for every action. Export-ready compliance reports."

Layout: Smaller than Policy pillar (col-span-6 or col-span-4 on desktop)

Keep it simple - static content, no animations needed.
  </action>
  <verify>
File exists and exports default component
  </verify>
  <done>
AuditPillar.tsx exists with report preview visual
  </done>
</task>

<task type="auto">
  <name>Task 4: Create PillarsSection index</name>
  <files>apps/www/components/PillarsSection/index.tsx</files>
  <action>
Create main PillarsSection component composing all three pillars.

Structure:
1. SectionContainer wrapper
2. Optional SectionHeader (Claude's discretion - reference Supabase pages for whether to include)
3. Grid layout with PolicyPillar taking featured position (more width)
4. IdentityPillar and AuditPillar side by side below or beside Policy

Layout options (pick based on visual balance):
- Option A: Policy full width, Identity + Audit in 2-col grid below
- Option B: Policy col-span-8, Identity + Audit stacked in col-span-4

Use responsive classes for mobile (all stack vertically).

Import and render all three pillar components.
  </action>
  <verify>
File exists at apps/www/components/PillarsSection/index.tsx
TypeScript compiles: `cd /Users/brad/Code/personal/axite/axite-new && npx tsc --noEmit 2>&1 | head -20`
  </verify>
  <done>
PillarsSection/index.tsx composes all three pillars with Policy featured prominently
  </done>
</task>

</tasks>

<verification>
- [ ] PolicyPillar.tsx exists with mini policy demo visual
- [ ] IdentityPillar.tsx exists with permission matrix visual
- [ ] AuditPillar.tsx exists with report preview visual
- [ ] PillarsSection/index.tsx composes all three
- [ ] Policy pillar has more visual weight than others
- [ ] TypeScript compiles without errors
</verification>

<success_criteria>
PillarsSection component renders all three pillars:
1. Enforceable Policy (featured, larger) with mini policy demo
2. Identity/Least Privilege (compact) with permission matrix
3. Audit Evidence (compact) with report preview

Policy pillar is visually emphasized over the other two.
</success_criteria>

<output>
After completion, create `.planning/phases/15-value-proposition/15-02-SUMMARY.md`
</output>
