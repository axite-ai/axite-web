---
phase: 15-value-proposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/www/components/TransformationSection/index.tsx
autonomous: true

must_haves:
  truths:
    - "2 transformation blocks visible showing pain to outcome"
    - "Each block has distinct pain statement and transformation outcome"
    - "Blocks use specific scenarios not abstract language"
  artifacts:
    - path: "apps/www/components/TransformationSection/index.tsx"
      provides: "Pain to transformation section"
      exports: ["default"]
  key_links:
    - from: "TransformationSection"
      to: "SectionContainer, Panel"
      via: "imports"
      pattern: "import.*SectionContainer|import.*Panel"
---

<objective>
Create TransformationSection component with 2 pain-to-outcome transformation blocks

Purpose: Communicate the problem-to-solution transformation using specific scenarios (e.g., "Agent tried to delete production DB" -> "Complete audit trail of every action")

Output: Standalone TransformationSection component ready for homepage integration
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-value-proposition/15-CONTEXT.md

Reference existing components:
@apps/www/components/Sections/HighlightColumns.tsx (column layout pattern)
@apps/www/components/Panel/Panel.tsx (card styling)
@apps/www/components/UI/SectionHeader.tsx (section header pattern)
@apps/www/components/Layouts/SectionContainer.tsx (section wrapper)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TransformationSection component</name>
  <files>apps/www/components/TransformationSection/index.tsx</files>
  <action>
Create TransformationSection component following Supabase patterns:

1. Use SectionContainer + SectionHeader wrapper (similar to ProofSection pattern)
2. Create two-column grid (1 col mobile, 2 cols desktop) for transformation blocks
3. Each block uses Panel component with:
   - "Before" state: pain statement with specific scenario
   - Arrow or visual separator
   - "After" state: transformation outcome

Content for 2 blocks (from CONTEXT decisions):

Block 1 - Control Crisis:
- Pain: "Agent tried to delete production database during routine cleanup"
- Outcome: "Every destructive action requires explicit policy approval with full audit trail"
- Visual hint: Use warning/alert icon for pain, checkmark for outcome

Block 2 - Compliance Scramble:
- Pain: "SOC2 auditor asks 'What can this agent access?' — no one knows"
- Outcome: "Complete inventory of every agent, tool, and permission — audit-ready"
- Visual hint: Use question/unknown icon for pain, shield for outcome

Design notes:
- Follow brand voice: calm, precise, mechanism-first
- Use foreground/foreground-light/foreground-lighter text hierarchy
- No useState/useEffect needed - pure static content
- Use Lucide icons for visual indicators (AlertTriangle, CheckCircle, HelpCircle, Shield)
  </action>
  <verify>
File exists at apps/www/components/TransformationSection/index.tsx
TypeScript compiles: `cd /Users/brad/Code/personal/axite/axite-new && npx tsc --noEmit 2>&1 | head -20`
  </verify>
  <done>
TransformationSection component exists with 2 pain-to-outcome blocks using Panel, SectionContainer, and SectionHeader
  </done>
</task>

</tasks>

<verification>
- [ ] Component file exists at TransformationSection/index.tsx
- [ ] Uses SectionContainer and SectionHeader
- [ ] Contains exactly 2 transformation blocks
- [ ] Each block has pain statement and outcome statement
- [ ] Uses specific scenarios (not abstract "problems")
- [ ] TypeScript compiles without errors
</verification>

<success_criteria>
TransformationSection component renders 2 transformation blocks showing:
1. "Control Crisis" - agent destructive action scenario
2. "Compliance Scramble" - audit/visibility scenario

Each block clearly shows pain -> outcome transformation with appropriate visual indicators.
</success_criteria>

<output>
After completion, create `.planning/phases/15-value-proposition/15-01-SUMMARY.md`
</output>
