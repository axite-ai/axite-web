---
phase: 15-value-proposition
plan: 03
type: execute
wave: 2
depends_on: ["15-01", "15-02"]
files_modified:
  - apps/www/pages/index.tsx
autonomous: true

must_haves:
  truths:
    - "TransformationSection visible on homepage"
    - "PillarsSection visible on homepage"
    - "Transformation section appears before Pillars section"
    - "All four phase 15 success criteria verifiable"
  artifacts:
    - path: "apps/www/pages/index.tsx"
      provides: "Homepage with Value Proposition sections"
      contains: ["TransformationSection", "PillarsSection"]
  key_links:
    - from: "apps/www/pages/index.tsx"
      to: "TransformationSection"
      via: "dynamic import"
      pattern: "dynamic.*TransformationSection"
    - from: "apps/www/pages/index.tsx"
      to: "PillarsSection"
      via: "dynamic import"
      pattern: "dynamic.*PillarsSection"
---

<objective>
Integrate TransformationSection and PillarsSection into homepage and verify phase success

Purpose: Complete Phase 15 by adding Value Proposition sections to the landing page

Output: Homepage displays transformation blocks and three pillars; all phase success criteria verified
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-value-proposition/15-CONTEXT.md
@.planning/phases/15-value-proposition/15-01-SUMMARY.md (if exists)
@.planning/phases/15-value-proposition/15-02-SUMMARY.md (if exists)

Reference:
@apps/www/pages/index.tsx (current homepage)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TransformationSection and PillarsSection to homepage</name>
  <files>apps/www/pages/index.tsx</files>
  <action>
Add both sections to homepage using dynamic imports (consistent with existing pattern).

1. Add dynamic imports at top:
```tsx
const TransformationSection = dynamic(() => import('~/components/TransformationSection'))
const PillarsSection = dynamic(() => import('~/components/PillarsSection'))
```

2. Add to JSX in correct order (per CONTEXT.md: transformations first, then pillars):
- After: ProofSection (existing)
- Before: Logos (existing)
- Order: TransformationSection, then PillarsSection

Current page structure:
Hero -> ProofSection -> Logos -> Products -> ...

Target structure:
Hero -> ProofSection -> TransformationSection -> PillarsSection -> Logos -> Products -> ...

Follow existing dynamic import pattern used for other sections.
  </action>
  <verify>
Build succeeds: `cd /Users/brad/Code/personal/axite/axite-new && pnpm build 2>&1 | tail -20`
  </verify>
  <done>
Homepage includes TransformationSection and PillarsSection in correct order
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify Phase 15 success criteria</name>
  <files></files>
  <action>
Verify all Phase 15 success criteria from ROADMAP.md:

1. "2-3 'current pain' to 'what changes' transformation blocks visible"
   - Check: TransformationSection contains 2 transformation blocks
   - Grep for pain/outcome content in TransformationSection/index.tsx

2. "Enforceable Policy pillar section with visual component present"
   - Check: PolicyPillar.tsx exists with policy demo visual

3. "Identity/Least Privilege pillar section with visual component present"
   - Check: IdentityPillar.tsx exists with permission matrix visual

4. "Audit Evidence pillar section with visual component present"
   - Check: AuditPillar.tsx exists with report preview visual

Run verification commands:
```bash
# Check transformation blocks exist
grep -l "pain\|outcome\|transformation" apps/www/components/TransformationSection/

# Check pillar files exist
ls apps/www/components/PillarsSection/

# Check homepage includes both sections
grep -E "TransformationSection|PillarsSection" apps/www/pages/index.tsx
```

Document verification results.
  </action>
  <verify>
All four success criteria confirmed in verification output
  </verify>
  <done>
Phase 15 success criteria verified:
1. 2 transformation blocks visible
2. Policy pillar with visual present
3. Identity pillar with visual present
4. Audit pillar with visual present
  </done>
</task>

</tasks>

<verification>
- [ ] TransformationSection imported dynamically in homepage
- [ ] PillarsSection imported dynamically in homepage
- [ ] Sections appear after ProofSection, before Logos
- [ ] Build succeeds without errors
- [ ] All 4 phase success criteria verified
</verification>

<success_criteria>
Phase 15 complete with all success criteria verified:
1. 2-3 "current pain" to "what changes" transformation blocks visible
2. Enforceable Policy pillar section with visual component present
3. Identity/Least Privilege pillar section with visual component present
4. Audit Evidence pillar section with visual component present
</success_criteria>

<output>
After completion, create `.planning/phases/15-value-proposition/15-03-SUMMARY.md`
</output>
