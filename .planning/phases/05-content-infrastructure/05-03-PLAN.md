---
phase: 05-content-infrastructure
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Blog index page displays list of posts with titles and dates"
    - "Blog post pages render MDX content with formatting"
    - "Code blocks have syntax highlighting"
  artifacts:
    - path: "apps/www/app/blog/BlogClient.tsx"
      provides: "Blog listing UI component"
      contains: "BlogClient"
    - path: "apps/www/app/blog/[slug]/BlogPostClient.tsx"
      provides: "Blog post content renderer"
      contains: "MDXRemote"
  key_links:
    - from: "apps/www/app/blog/BlogClient.tsx"
      to: "initialBlogs"
      via: "props"
      pattern: "initialBlogs"
---

<objective>
Verify blog content renders correctly with human visual inspection.

Purpose: Automated checks verify routes return 200, but cannot confirm visual rendering - proper layout, MDX formatting, syntax highlighting colors, and post metadata display. Human verification confirms the content system is actually working.

Output: Human-verified blog functionality with visual confirmation
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-content-infrastructure/05-CONTEXT.md
@.planning/phases/05-content-infrastructure/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and prepare test URLs</name>
  <files></files>
  <action>
  Start the development server and identify test URLs:

  1. Start dev server: `pnpm dev`
  2. Note the port (likely 3000 or 3006)
  3. Prepare URLs for testing:
     - Blog index: http://localhost:PORT/blog
     - A blog post with code: Find one with code blocks in _blog/
     - A blog post with images: Find one with images

  Suggested test posts (search _blog/ directory):
  - `supabase-alpha-april-2020` - Simple post, good for basic test
  - `postgresql-templates` - Has code blocks for syntax highlighting
  - Find a post with significant content for layout verification
  </action>
  <verify>
  Dev server runs:
  ```
  cd apps/www && pnpm dev &
  sleep 10
  lsof -i :3000 -i :3006 | grep -E "LISTEN" || echo "Check server port"
  ```
  </verify>
  <done>Dev server running, test URLs identified</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Blog content infrastructure with static MDX posts</what-built>
  <how-to-verify>
  **Test 1: Blog Index Page**
  1. Visit http://localhost:PORT/blog (use actual port from dev server)
  2. Verify:
     - Page loads without errors
     - List of blog posts displays
     - Each post shows title and date
     - Posts are sorted by date (newest first)

  **Test 2: Individual Blog Post**
  1. Click on any post (or visit http://localhost:PORT/blog/supabase-alpha-april-2020)
  2. Verify:
     - Post content renders
     - Title and metadata display
     - Body text is readable and formatted
     - Links work (internal/external)

  **Test 3: Code Syntax Highlighting**
  1. Find a post with code blocks (or visit one with known code)
  2. Verify:
     - Code blocks have syntax highlighting (colors, not monochrome)
     - Line numbers display (if enabled)
     - Code is readable with proper indentation

  **Test 4: Navigation**
  1. From a blog post, click "Back to blog" or navigate to /blog
  2. Verify navigation works without errors
  </how-to-verify>
  <resume-signal>Type "approved" if blog renders correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. Blog index shows list of posts
2. Individual posts render MDX content
3. Code blocks have syntax highlighting
4. Navigation between pages works
</verification>

<success_criteria>
- Human confirms blog index shows posts with metadata
- Human confirms blog posts render MDX content
- Human confirms code blocks have syntax highlighting
- No visual rendering issues observed
</success_criteria>

<output>
After completion, create `.planning/phases/05-content-infrastructure/05-03-SUMMARY.md`
</output>
