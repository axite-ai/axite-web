---
phase: 06-page-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Homepage renders at localhost with 200 status"
    - "Pricing page renders at /pricing with 200 status"
    - "Nav component loads in page HTML"
    - "Footer component loads in page HTML"
  artifacts: []
  key_links:
    - from: "pages/index.tsx"
      to: "components/Layouts/Default.tsx"
      via: "Layout wrapper"
      pattern: "DefaultLayout|<Layout"
    - from: "components/Layouts/Default.tsx"
      to: "components/Nav/index.tsx"
      via: "Nav import"
      pattern: "import Nav from"
    - from: "components/Layouts/Default.tsx"
      to: "components/Footer/index.tsx"
      via: "Footer import"
      pattern: "import.*Footer"
---

<objective>
Verify that the homepage and pricing page render correctly with the Default Layout (Nav + Footer).

Purpose: Confirm that the extracted Supabase www site structure works as a standalone app - pages load with navigation and footer.
Output: Verification that dev server serves pages with 200 status and expected content.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and verify homepage renders</name>
  <files>apps/www/pages/index.tsx, apps/www/components/Layouts/Default.tsx</files>
  <action>
Start the dev server and verify homepage loads:

1. Start dev server: `cd apps/www && pnpm dev` (run in background)
2. Wait for server to be ready (check for "Ready in" message or port listening)
3. Curl homepage and verify:
   - HTTP 200 status
   - Response contains "Supabase" (brand name - confirms real content loaded)
   - Response contains navigation indicators (e.g., "Product", "Developers", "Pricing")
   - Response contains footer indicators (e.g., "SOC2" or social icons markup)

Expected: Homepage loads with full layout including Nav and Footer components.
  </action>
  <verify>
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000 returns 200
curl -s http://localhost:3000 | grep -q "Supabase" returns 0
curl -s http://localhost:3000 | grep -q "Product" returns 0
  </verify>
  <done>Homepage returns 200 with content containing brand name and navigation elements</done>
</task>

<task type="auto">
  <name>Task 2: Verify pricing page renders</name>
  <files>apps/www/pages/pricing.tsx</files>
  <action>
With dev server running, verify pricing page loads:

1. Curl /pricing endpoint and verify:
   - HTTP 200 status
   - Response contains pricing-related content (e.g., "Pricing", "Plan", "Free")
   - Response uses same Layout (evidence of Nav/Footer present)

Expected: Pricing page loads successfully with layout components.
  </action>
  <verify>
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/pricing returns 200
curl -s http://localhost:3000/pricing | grep -qi "pricing" returns 0
  </verify>
  <done>Pricing page returns 200 with pricing-related content</done>
</task>

<task type="auto">
  <name>Task 3: Verify blog page renders (cross-check with Phase 5)</name>
  <files>apps/www/app/blog/page.tsx</files>
  <action>
Verify blog index page loads (quick sanity check as blog was verified in Phase 5):

1. Curl /blog endpoint and verify:
   - HTTP 200 status
   - Response contains blog content markers

This confirms the app router pages (blog) and pages router pages (homepage, pricing) both work.
  </action>
  <verify>
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/blog returns 200
  </verify>
  <done>Blog page returns 200 (app router works alongside pages router)</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. All three page endpoints return HTTP 200
2. Homepage contains expected brand and navigation content
3. Pricing page loads with pricing-specific content
4. Blog page loads (confirming both routing systems work)
5. No server errors in console during page loads
</verification>

<success_criteria>
- [ ] Homepage (/) returns 200 with Supabase branding and nav elements
- [ ] Pricing page (/pricing) returns 200 with pricing content
- [ ] Blog page (/blog) returns 200
- [ ] Dev server runs without fatal errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-page-structure/06-01-SUMMARY.md`
</output>
