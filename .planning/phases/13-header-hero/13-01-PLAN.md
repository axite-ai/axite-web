---
phase: 13-header-hero
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/www/components/Nav/index.tsx
  - apps/www/data/nav.tsx
autonomous: true

must_haves:
  truths:
    - "Header shows only Axite logo with no navigation links"
    - "Header has single 'Book Security Review' CTA button"
    - "Mobile menu removed or empty (no nav items to show)"
  artifacts:
    - path: "apps/www/components/Nav/index.tsx"
      provides: "Minimal header with logo and CTA only"
      contains: "Book Security Review"
    - path: "apps/www/data/nav.tsx"
      provides: "Empty navigation data"
      contains: "primaryNav: []"
  key_links:
    - from: "apps/www/components/Nav/index.tsx"
      to: "Calendly placeholder"
      via: "Link href"
      pattern: "href.*contact/sales|href.*calendly"
---

<objective>
Strip the header to minimal branding: logo + single CTA button.

Purpose: Landing page header should focus attention, not provide navigation. Following YC-style pattern where header has logo and single action.

Output: Simplified Nav component with no navigation links, only logo and "Book Security Review" CTA.
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-header-hero/13-CONTEXT.md
@apps/www/components/Nav/index.tsx
@apps/www/data/nav.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clear navigation data</name>
  <files>apps/www/data/nav.tsx</files>
  <action>
Update `apps/www/data/nav.tsx` to return empty primaryNav array:

```typescript
export const getMenu = () => ({
  primaryNav: [],
})
```

This removes Docs, Blog, Trust links from the navigation. These pages still exist but won't be in header nav.
  </action>
  <verify>File contains `primaryNav: []` - empty array</verify>
  <done>Navigation data exports empty primaryNav array</done>
</task>

<task type="auto">
  <name>Task 2: Simplify header to logo + CTA</name>
  <files>apps/www/components/Nav/index.tsx</files>
  <action>
Modify `apps/www/components/Nav/index.tsx`:

1. Remove the entire NavigationMenu section (lines ~115-148 with the menu.primaryNav.map)

2. Remove the GitHubButton component from the header right side

3. Replace the entire isLoggedIn ternary block (lines ~153-188) with a single CTA:
   ```tsx
   <Button className="hidden lg:block" asChild>
     <Link
       href="/contact/sales"
       onClick={() =>
         sendTelemetryEvent({
           action: 'book_security_review_clicked',
           properties: { buttonLocation: 'Header Nav' },
         })
       }
     >
       Book Security Review
     </Link>
   </Button>
   ```

4. Remove the HamburgerButton and MobileMenu components since there are no nav items:
   - Remove the `{!open && <HamburgerButton ... />}` block
   - Remove the `<MobileMenu ... />` component
   - Remove the `open` and `setOpen` state variables (they become unused)
   - Remove the `useEffect` blocks that depend on `open`

5. Clean up unused imports:
   - Remove: `GitHubButton`, `HamburgerButton`, `MenuItem`, `MobileMenu`
   - Remove: `NavigationMenu`, `NavigationMenuContent`, `NavigationMenuItem`, `NavigationMenuLink`, `NavigationMenuList`, `NavigationMenuTrigger`
   - Remove: `useDropdownMenu`, `AuthenticatedDropdownMenu`, `useIsLoggedIn`, `useUser`
   - Keep: `Button`, `buttonVariants`, `cn` from 'ui'
   - Keep: `RightClickBrandLogo`
   - Keep: `Link` from 'next/link'
   - Keep: `useSendTelemetryEvent`
   - Keep: `getMenu` (even though it returns empty array - maintains pattern)
   - Keep: `usePathname`, `useWindowSize` if still needed for sticky nav logic

6. The header structure should become:
   ```tsx
   <div className="flex items-center px-6 lg:px-0 flex-1 sm:items-stretch justify-between">
     <div className="flex items-center">
       <div className="flex items-center flex-shrink-0">
         <RightClickBrandLogo />
       </div>
     </div>
     <div className="flex items-center gap-2 opacity-0 animate-fade-in !scale-100 delay-300">
       <Button className="hidden lg:block" asChild>
         <Link href="/contact/sales" ...>
           Book Security Review
         </Link>
       </Button>
     </div>
   </div>
   ```

Keep all the sticky nav logic, background treatments, and scroll progress - those are good patterns.
  </action>
  <verify>
Run `npm run build --filter=www` - should complete without errors.
Visually verify: header shows logo on left, "Book Security Review" button on right, no nav links between.
  </verify>
  <done>Header displays only logo and single CTA button, no navigation links</done>
</task>

</tasks>

<verification>
1. `grep -q "primaryNav: \[\]" apps/www/data/nav.tsx` returns success
2. Header renders with logo and single CTA only
3. No navigation menu items visible in header
4. Build completes without errors
</verification>

<success_criteria>
- Header shows Axite logo (left) and "Book Security Review" button (right)
- No navigation links (Docs, Blog, Trust) in header
- Mobile hamburger menu removed (no items to show)
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-header-hero/13-01-SUMMARY.md`
</output>
