---
phase: 10-content-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/www/components/Products/index.tsx
  - apps/www/data/home/ProductModules.tsx
autonomous: true

must_haves:
  truths:
    - "Products section shows three Axite pillars: Policy, Identity, Audit"
    - "Each pillar card has relevant description for agent governance"
    - "No Supabase product cards (Database, Auth, Storage, etc.)"
    - "Cards link to /product#policy, /product#identity, /product#audit"
  artifacts:
    - path: "apps/www/components/Products/index.tsx"
      provides: "Homepage product cards for Axite pillars"
      contains: "Policy Enforcement"
    - path: "apps/www/data/home/ProductModules.tsx"
      provides: "Product module data for homepage"
      contains: "Audit"
  key_links:
    - from: "apps/www/pages/index.tsx"
      to: "apps/www/components/Products/index.tsx"
      via: "dynamic import"
      pattern: "Products"
---

<objective>
Update homepage products section to display Axite governance pillars.

Purpose: Showcase the three pillars (Policy, Identity, Audit) as main product features
Output: Products section with Axite-specific cards instead of Supabase products
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-content-pages/10-CONTEXT.md
@apps/www/components/Products/index.tsx
@apps/www/data/home/ProductModules.tsx
@apps/www/data/MainProducts.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Axite product data file for homepage</name>
  <files>apps/www/data/home/ProductModules.tsx</files>
  <action>
Create a new product module data file that defines the three Axite pillars.

Create the file with the following content:

```tsx
export type ProductModuleType = {
  [key: string]: {
    name: string
    icon: string
    description: string | JSX.Element
    description_short: string
    label: string
    url: string
  }
}

// Axite governance pillars for homepage
const ProductModules: ProductModuleType = {
  policy: {
    name: 'Policy Enforcement',
    icon: 'M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z',
    description: (
      <>
        <strong>Allow, deny, or require approval</strong> for any agent action at the gateway level. Centralized rules, enforced consistently.
      </>
    ),
    description_short: 'Control what agents can do',
    label: '',
    url: '/product#policy',
  },
  identity: {
    name: 'Identity & RBAC',
    icon: 'M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z',
    description: (
      <>
        <strong>Role-based access control</strong> for agents, tools, and actions. Least-privilege by default, escalation when needed.
      </>
    ),
    description_short: 'RBAC for agents and tools',
    label: '',
    url: '/product#identity',
  },
  audit: {
    name: 'Audit Trails',
    icon: 'M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-3 7h3m-3 4h3m-6-4h.01M9 16h.01',
    description: (
      <>
        <strong>Complete decision traces</strong> and logs for every agent action. Compliance-ready evidence your security team needs.
      </>
    ),
    description_short: 'Audit-grade evidence and logs',
    label: '',
    url: '/product#audit',
  },
}

export default ProductModules
```

Key changes:
- Replace Supabase modules (Vector, etc.) with Axite pillars
- Three governance pillars: Policy, Identity, Audit
- Each has icon (Heroicons-style SVG path), description, and URL
- URLs point to /product#policy, /product#identity, /product#audit (anchor links to product page sections)
  </action>
  <verify>
Run: `grep "Policy Enforcement" apps/www/data/home/ProductModules.tsx`
Verify: Policy pillar defined
Run: `grep "Identity & RBAC" apps/www/data/home/ProductModules.tsx`
Verify: Identity pillar defined
Run: `grep "Audit Trails" apps/www/data/home/ProductModules.tsx`
Verify: Audit pillar defined
Run: `grep -c "Vector\|Cron\|Queues" apps/www/data/home/ProductModules.tsx`
Verify: Returns 0 (Supabase modules removed)
  </verify>
  <done>Product modules data contains Axite governance pillars</done>
</task>

<task type="auto">
  <name>Task 2: Update Products component for Axite pillars</name>
  <files>apps/www/components/Products/index.tsx</files>
  <action>
Replace the Products component to display Axite governance pillars instead of Supabase products.

Replace the entire file with:

```tsx
import { Check, Shield, Users, ClipboardList } from 'lucide-react'

import SectionContainer from '~/components/Layouts/SectionContainer'
import ProductCard from './ProductCard'

import ProductModules from '~/data/home/ProductModules'
import { useSendTelemetryEvent } from '~/lib/telemetry'

interface Props {
  products?: any
}

const Products: React.FC<Props> = () => {
  const _sendTelemetryEvent = useSendTelemetryEvent()

  const sendTelemetryEvent = async (product: string) => {
    return await _sendTelemetryEvent({
      action: 'homepage_product_card_clicked',
      properties: { product },
    })
  }

  return (
    <SectionContainer className="!pt-0 grid grid-cols-1 gap-4 sm:grid-cols-2 md:gap-4 xl:gap-3 2xl:gap-6 md:grid-cols-12">
      {/* Overview Card - spans full width on mobile, 6 cols on desktop */}
      <ProductCard
        className="col-span-6 md:col-span-12 xl:col-span-6"
        alignLeft
        url="/product"
        icon="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"
        title="Agent Governance Platform"
        subtitle={
          <>
            The <strong>control plane for AI agents</strong>. Policy enforcement, identity management, and audit trails in one platform.
          </>
        }
        highlights={
          <ul className="flex flex-col gap-1 text-sm">
            <li>
              <Check className="inline h-4 w-4" /> MCP-native gateway
            </li>
            <li>
              <Check className="inline h-4 w-4" /> Works with any agent framework
            </li>
            <li>
              <Check className="inline h-4 w-4" /> Deploy anywhere
            </li>
          </ul>
        }
        onClick={() => sendTelemetryEvent('overview')}
      />

      {/* Policy Enforcement */}
      <ProductCard
        className="col-span-6 xl:col-span-3"
        alignLeft
        url={ProductModules.policy.url}
        icon={ProductModules.policy.icon}
        title={ProductModules.policy.name}
        subtitle={ProductModules.policy.description}
        onClick={() => sendTelemetryEvent('policy')}
      />

      {/* Identity & RBAC */}
      <ProductCard
        className="col-span-6 xl:col-span-3"
        alignLeft
        url={ProductModules.identity.url}
        icon={ProductModules.identity.icon}
        title={ProductModules.identity.name}
        subtitle={ProductModules.identity.description}
        onClick={() => sendTelemetryEvent('identity')}
      />

      {/* Audit Trails */}
      <ProductCard
        className="col-span-6 xl:col-span-3"
        alignLeft
        url={ProductModules.audit.url}
        icon={ProductModules.audit.icon}
        title={ProductModules.audit.name}
        subtitle={ProductModules.audit.description}
        onClick={() => sendTelemetryEvent('audit')}
      />

      {/* MCP Gateway Card */}
      <ProductCard
        className="col-span-6 xl:col-span-3"
        alignLeft
        url="/product#gateway"
        icon="M5 12h14M5 12a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v4a2 2 0 01-2 2M5 12a2 2 0 00-2 2v4a2 2 0 002 2h14a2 2 0 002-2v-4a2 2 0 00-2-2"
        title="MCP Gateway"
        subtitle={
          <>
            <strong>Route all agent-to-tool communication</strong> through a governed MCP proxy with built-in policy checks.
          </>
        }
        onClick={() => sendTelemetryEvent('gateway')}
      />

      <p className="text-xl sm:text-2xl text-foreground-lighter col-span-full tracking-[-.01rem]">
        <span className="text-foreground">One platform.</span> Policy, identity, and audit for every agent action.
      </p>
    </SectionContainer>
  )
}

export default Products
```

Key changes:
- Remove all Supabase product cards (Database, Auth, Storage, etc.)
- Remove visual imports (DatabaseVisual, AuthVisual, etc.)
- Add overview card spanning 6 columns
- Add three pillar cards: Policy, Identity, Audit
- Add MCP Gateway card
- Update tagline at bottom
- Remove unused imports and visual components
- Keep ProductCard component (reusing existing layout)
  </action>
  <verify>
Run: `grep "Agent Governance Platform" apps/www/components/Products/index.tsx`
Verify: Overview card title appears
Run: `grep "Policy Enforcement" apps/www/components/Products/index.tsx`
Verify: Policy card referenced
Run: `grep -c "DatabaseVisual\|AuthVisual\|StorageVisual" apps/www/components/Products/index.tsx`
Verify: Returns 0 (old visuals removed)
Run: `grep "One platform" apps/www/components/Products/index.tsx`
Verify: New tagline appears
  </verify>
  <done>Products component displays Axite governance pillars</done>
</task>

</tasks>

<verification>
1. Run the dev server: `cd apps/www && pnpm dev`
2. Visit homepage at http://localhost:3000
3. Scroll to products section
4. Verify "Agent Governance Platform" overview card is visible
5. Verify three pillar cards: Policy Enforcement, Identity & RBAC, Audit Trails
6. Verify MCP Gateway card is visible
7. Verify tagline "One platform. Policy, identity, and audit for every agent action."
8. Click each card and verify links navigate to /product page sections (anchor links)
</verification>

<success_criteria>
- Homepage shows Axite governance pillars (Policy, Identity, Audit)
- Overview card explains "control plane for AI agents"
- MCP Gateway card present
- No Supabase product cards visible
- Cards link to appropriate /product/* URLs
</success_criteria>

<output>
After completion, create `.planning/phases/10-content-pages/10-02-SUMMARY.md`
</output>
