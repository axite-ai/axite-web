---
phase: 11-blog-compatibility
plan: 03
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - apps/www/_blog/*.mdx (deletion of 387 files)
autonomous: true

must_haves:
  truths:
    - "All Supabase blog posts are deleted from _blog directory"
    - "_blog directory is empty or contains only Axite posts"
    - "No Supabase-specific MDX content remains"
  artifacts:
    - path: "apps/www/_blog/"
      provides: "Empty directory ready for Axite content"
---

<objective>
Delete all existing Supabase blog posts to prepare for Axite content.

Purpose: Remove 387 Supabase blog posts before adding Axite placeholder posts
Output: Empty _blog directory ready for new content
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-blog-compatibility/11-CONTEXT.md
@.planning/phases/11-blog-compatibility/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete all Supabase blog posts</name>
  <files>apps/www/_blog/*.mdx</files>
  <action>
Delete all 387 existing Supabase blog posts from the _blog directory.

```bash
rm apps/www/_blog/*.mdx
```

This removes all MDX files containing Supabase content. The directory structure will be preserved for the new Axite posts.

Note: This is intentional content replacement per the project requirements. The Supabase blog posts are being replaced with Axite placeholder content.
  </action>
  <verify>
Run: `ls apps/www/_blog/*.mdx 2>/dev/null | wc -l`
Verify: Returns 0 (no MDX files remain)
Run: `ls -la apps/www/_blog/`
Verify: Directory exists but contains no .mdx files
  </verify>
  <done>All Supabase blog posts deleted</done>
</task>

</tasks>

<verification>
1. Run `ls apps/www/_blog/*.mdx 2>/dev/null | wc -l` returns 0
2. The _blog directory still exists
3. No Supabase content remains in the blog
</verification>

<success_criteria>
- Zero MDX files in apps/www/_blog/
- _blog directory still exists (not deleted)
- Ready for new Axite posts
</success_criteria>

<output>
After completion, create `.planning/phases/11-blog-compatibility/11-03-SUMMARY.md`
</output>
