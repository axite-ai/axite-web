---
phase: 11-blog-compatibility
plan: 06
type: execute
wave: 3
depends_on: [11-01, 11-02, 11-04, 11-05]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Blog index page loads without errors"
    - "Blog index page lists all 10 Axite posts"
    - "Individual blog posts render correctly"
    - "Blog header shows Axite branding (colors/typography from Phase 8)"
    - "No Supabase text visible in blog UI"
    - "Category filtering works with Axite posts"
    - "Tag filtering works with Axite posts"
  artifacts: []
---

<objective>
Verify the blog functions correctly with Axite branding and new content.

Purpose: Ensure all blog functionality works after content replacement
Output: Verified working blog with Axite content and branding
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-blog-compatibility/11-CONTEXT.md
@.planning/phases/11-blog-compatibility/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify blog builds without errors</name>
  <files></files>
  <action>
Run a production build to verify no MDX or build errors:

```bash
cd apps/www && pnpm build 2>&1 | head -100
```

Look for:
- No MDX compilation errors
- No missing author errors
- No image path errors
- Successful build completion

If errors occur, identify the failing file and fix:
- MDX syntax errors: Check for non-JSX-compliant tags
- Author errors: Verify axite_team exists in authors.json
- Image errors: Verify placeholder images exist
  </action>
  <verify>
Run: `cd apps/www && pnpm build 2>&1 | tail -20`
Verify: Build completes without errors
  </verify>
  <done>Blog builds successfully</done>
</task>

<task type="auto">
  <name>Task 2: Verify blog index page functionality</name>
  <files></files>
  <action>
Start the dev server and verify blog index page:

```bash
cd apps/www && pnpm dev &
sleep 10
curl -s http://localhost:3000/blog | head -100
```

Verify:
1. Page loads without errors
2. Page title contains "Axite"
3. Blog posts are listed
4. No Supabase text visible

Manual verification (if dev server available):
- Navigate to http://localhost:3000/blog
- Check that 10 Axite posts appear
- Check that featured post shows correctly
- Check that Axite branding is visible in header
  </action>
  <verify>
Run: `curl -s http://localhost:3000/blog 2>/dev/null | grep -c "Axite" || echo "Server not running"`
Verify: Axite appears in page content (or server needs to be started)
  </verify>
  <done>Blog index page loads correctly</done>
</task>

<task type="auto">
  <name>Task 3: Verify individual blog post rendering</name>
  <files></files>
  <action>
Verify individual blog posts render correctly:

```bash
# Check that the introducing-axite post exists
curl -s http://localhost:3000/blog/introducing-axite 2>/dev/null | head -50
```

For each post, verify:
1. Post page loads without errors
2. Title renders correctly
3. Author shows "Axite Team"
4. Code blocks render with syntax highlighting
5. Admonitions render correctly
6. Table of contents generates

Test a sample of posts:
- /blog/introducing-axite (has overview content)
- /blog/getting-started-with-axite-policies (has code blocks)
- /blog/audit-trails-complete-visibility (has tables)
  </action>
  <verify>
Run: `ls apps/www/_blog/*.mdx | wc -l`
Verify: Returns 10 (all posts exist)
Run: `grep -l "axite_team" apps/www/_blog/*.mdx | wc -l`
Verify: Returns 10 (all posts use axite_team author)
  </verify>
  <done>Individual posts render correctly</done>
</task>

<task type="auto">
  <name>Task 4: Verify category and tag filtering</name>
  <files></files>
  <action>
Verify category and tag pages work:

```bash
# Check categories used in posts
grep -h "categories:" apps/www/_blog/*.mdx -A 2 | grep "  - " | sort | uniq -c
```

Expected categories:
- product (4 posts)
- developers (2 posts)
- engineering (2 posts)
- company (2 posts)

Verify category pages:
- /blog/categories/product
- /blog/categories/developers
- /blog/categories/engineering
- /blog/categories/company

Verify tag pages for common tags:
- /blog/tags/governance
- /blog/tags/policy
- /blog/tags/security
  </action>
  <verify>
Run: `grep -h "categories:" apps/www/_blog/*.mdx -A 2 | grep "  - " | sort | uniq`
Verify: Shows product, developers, engineering, company
Run: `grep -h "tags:" apps/www/_blog/*.mdx -A 5 | grep "  - " | sort | uniq | head -10`
Verify: Shows relevant tags like governance, policy, security
  </verify>
  <done>Category and tag filtering works</done>
</task>

<task type="auto">
  <name>Task 5: Final verification - no Supabase references</name>
  <files></files>
  <action>
Final check to ensure no Supabase references remain in visible blog content:

```bash
# Check blog posts
grep -ri "supabase" apps/www/_blog/ || echo "No Supabase in posts"

# Check blog page metadata
grep -ri "supabase" apps/www/app/blog/ || echo "No Supabase in pages"

# Check authors (should only have old authors, not used by new posts)
# The authors.json still has old authors - that's fine, they're just unused
```

Verify:
1. No Supabase text in blog posts
2. No Supabase text in blog page metadata
3. Blog displays Axite branding throughout
  </action>
  <verify>
Run: `grep -ri "supabase" apps/www/_blog/*.mdx 2>/dev/null | wc -l`
Verify: Returns 0 (no Supabase in posts)
Run: `grep -i "supabase" apps/www/app/blog/page.tsx apps/www/app/blog/BlogClient.tsx 2>/dev/null | wc -l`
Verify: Returns 0 (no Supabase in blog pages)
  </verify>
  <done>No Supabase references in visible blog content</done>
</task>

</tasks>

<verification>
1. Blog builds without errors
2. Blog index page loads and lists 10 Axite posts
3. Individual blog posts render with correct formatting
4. Category pages show filtered posts
5. Tag pages show filtered posts
6. No Supabase text visible anywhere in blog
7. Axite branding (colors, typography) visible in blog
</verification>

<success_criteria>
- Production build completes without errors
- Blog index page loads and displays posts
- All 10 posts render individually without errors
- Category filtering shows correct posts
- Tag filtering shows correct posts
- Zero visible Supabase references in blog
- Axite branding consistent throughout
</success_criteria>

<output>
After completion, create `.planning/phases/11-blog-compatibility/11-06-SUMMARY.md`
</output>
