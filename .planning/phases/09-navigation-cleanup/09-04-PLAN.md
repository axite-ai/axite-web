---
phase: 09-navigation-cleanup
plan: 04
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - apps/www/components/Nav/MobileMenu.tsx
  - .planning/phases/09-navigation-cleanup/REMOVED-PAGES.md
autonomous: true

must_haves:
  truths:
    - "Mobile menu shows simplified Axite navigation"
    - "Mobile menu Product accordion shows Axite features"
    - "No Supabase text in mobile menu (Explore everything you can do with Supabase)"
    - "Removed pages documented in REMOVED-PAGES.md"
  artifacts:
    - path: "apps/www/components/Nav/MobileMenu.tsx"
      provides: "Mobile navigation with Axite structure"
      contains: "Get started"
    - path: ".planning/phases/09-navigation-cleanup/REMOVED-PAGES.md"
      provides: "Documentation of removed Supabase pages"
      contains: "/auth"
  key_links:
    - from: "apps/www/components/Nav/MobileMenu.tsx"
      to: "apps/www/data/nav.tsx"
      via: "menu prop"
      pattern: "menu.primaryNav"
---

<objective>
Update mobile navigation for Axite and create documentation of removed pages.

Purpose: Ensure mobile navigation matches desktop, document what pages were removed from nav
Output: Consistent mobile experience with Axite features, tracking doc for removed content
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-navigation-cleanup/09-CONTEXT.md
@apps/www/components/Nav/MobileMenu.tsx
@apps/www/data/nav.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update mobile menu for Axite navigation</name>
  <files>apps/www/components/Nav/MobileMenu.tsx</files>
  <action>
Update the MobileMenu component to work with simplified Axite navigation:

1. Update the AccordionMenuItem component (around lines 56-172) to handle simplified structure:

Replace the entire AccordionMenuItem component with:
```tsx
const AccordionMenuItem = ({ menuItem }: any) => (
  <>
    {menuItem.title === 'Product' ? (
      <>
        {/* Main Products */}
        {Object.values(menuItem.subMenu || MainProductsData)?.map((component: any) => (
          <MenuItem
            key={component.name}
            title={component.name}
            href={component.url}
            description={component.description_short}
            icon={component.icon}
          />
        ))}
        {/* Product Modules */}
        <div>
          <div className="group flex items-center p-2 text-foreground-lighter text-xs uppercase tracking-widest font-mono">
            Resources
          </div>
          <ul className="flex flex-col gap-0">
            {Object.values(ProductModulesData).map((productModule) => (
              <MenuItem
                key={productModule.name}
                title={productModule.name}
                href={productModule.url}
                description={productModule.description_short}
                icon={productModule.icon}
              />
            ))}
          </ul>
        </div>
        <Link
          href="/docs/quickstart"
          className="
            flex items-center justify-between group text-sm
            p-4 mt-4 gap-2
            rounded-lg border
            bg-alternative-200 text-foreground-light
            hover:text-foreground hover:border-foreground-muted
            focus-visible:text-foreground focus-visible:ring-2 focus-visible:outline-none
            focus-visible:rounded focus-visible:ring-foreground-lighter
          "
        >
          <div className="flex flex-col gap-1 !leading-3">
            <span>Get started</span>
            <span className="text-foreground-lighter text-xs leading-4">
              Add governance to your AI agents in minutes.
            </span>
          </div>
          <ChevronRight
            strokeWidth={2}
            className="w-3 -ml-1 transition-all will-change-transform -translate-x-1 opacity-80 group-hover:translate-x-0 group-hover:opacity-100"
          />
        </Link>
      </>
    ) : null}
  </>
)
```

2. Remove the Developers and Solutions cases since they no longer have dropdowns.
The nav.tsx now only has Product with a dropdown, others are direct links.

3. Update the bottom CTA buttons (around lines 266-315):
Replace the sign-in and start project buttons with Axite equivalents:
```tsx
<div className="absolute bottom-0 left-0 right-0 top-auto w-full bg-alternative flex items-stretch p-4 gap-4">
  {!isUserLoading && (
    <>
      <Link href="/contact/sales" passHref legacyBehavior>
        <Button block type="default" asChild>
          <a type={undefined} className="h-10 py-4">
            Contact sales
          </a>
        </Button>
      </Link>
      <Link href="/docs/quickstart" passHref legacyBehavior>
        <Button block asChild>
          <a type={undefined} className="h-10 py-4">
            Get started
          </a>
        </Button>
      </Link>
    </>
  )}
</div>
```

4. Update logo alt text (lines 227-238):
Change alt="Supabase Logo" to alt="Axite Logo" in both Image components.

5. Remove Supabase-specific text:
- "Explore everything you can do with Supabase." should become "Add governance to your AI agents in minutes."
- Remove staticContent import for jobsCount (no longer needed)
- Remove jobs counter logic

6. Keep the Supabase logo images for now (defer to future milestone).

7. Remove unused imports at the top:
- Remove `import { data as DevelopersData } from 'data/Developers'`
- Remove `import { navData as SolutionsData } from 'data/Solutions'`
- Keep MainProductsData and ProductModulesData imports
  </action>
  <verify>
Run: `grep "Supabase" apps/www/components/Nav/MobileMenu.tsx`
Verify: Only appears in logo import paths (acceptable - logo deferred)
Run: `grep "Get started" apps/www/components/Nav/MobileMenu.tsx`
Verify: Appears in CTA button and description
Run: `grep "Contact sales" apps/www/components/Nav/MobileMenu.tsx`
Verify: Appears in button
Run: `grep "Axite Logo" apps/www/components/Nav/MobileMenu.tsx`
Verify: Alt text updated
  </verify>
  <done>Mobile menu shows Axite navigation with updated CTAs</done>
</task>

<task type="auto">
  <name>Task 2: Create removed pages documentation</name>
  <files>.planning/phases/09-navigation-cleanup/REMOVED-PAGES.md</files>
  <action>
Create a tracking document for pages removed from navigation:

```markdown
# Removed Pages - Phase 9

**Created:** 2026-01-23
**Purpose:** Track Supabase pages removed from navigation (files kept in repo for reference)

## Strategy

Per Phase 9 context decisions:
- Old Supabase product URLs return hard 404
- Page files kept in repo for reference
- Only navigation links removed, not page files

## Pages Removed from Navigation

### Supabase Products
These product pages are no longer linked in navigation:

| Page | URL | Status |
|------|-----|--------|
| Database | /database | Unlinked |
| Auth | /auth | Unlinked |
| Storage | /storage | Unlinked |
| Edge Functions | /edge-functions | Unlinked |
| Realtime | /realtime | Unlinked |
| Vector | /modules/vector | Unlinked |
| Cron | /modules/cron | Unlinked |
| Queues | /modules/queues | Unlinked |

### Solutions Pages
These solution pages are no longer linked in navigation:

| Page | URL | Status |
|------|-----|--------|
| AI Builders | /solutions/ai-builders | Unlinked |
| No Code | /solutions/no-code | Unlinked |
| Beginners | /solutions/beginners | Unlinked |
| Developers | /solutions/developers | Unlinked |
| Postgres Developers | /solutions/postgres-developers | Unlinked |
| Vibe Coders | /solutions/vibe-coders | Unlinked |
| Hackathon | /solutions/hackathon | Unlinked |
| Startups | /solutions/startups | Unlinked |
| Agencies | /solutions/agencies | Unlinked |
| Innovation Teams | /solutions/innovation-teams | Unlinked |

### Migration Pages
These comparison/migration pages are no longer linked:

| Page | URL | Status |
|------|-----|--------|
| Switch from Firebase | /solutions/switch-from-firebase | Unlinked |
| Switch from Neon | /solutions/switch-from-neon | Unlinked |
| Switch from Convex | /solutions/switch-from-convex | Unlinked |

### Other Supabase Pages
Additional Supabase-specific pages removed from navigation:

| Page | URL | Status |
|------|-----|--------|
| Launch Week | /launch-week | Unlinked |
| GA Week | /ga-week | Unlinked |
| Open Source | /open-source | Unlinked |
| SupaSquad | /open-source/contributing/supasquad | Unlinked |
| Partners | /partners | Unlinked |
| Integrations | /partners/integrations | Unlinked |
| Brand Assets | /brand-assets | Unlinked |
| Customers | /customers | Unlinked |
| Features (Supabase) | /features | Unlinked |
| Next.js | /nextjs | Unlinked |
| Alternatives | /alternatives/* | Unlinked |
| Events | /events/* | Unlinked |

## Blog Content

Blog content is **preserved** per project requirements:
- Blog index (/blog) remains linked
- Individual blog posts remain accessible
- Supabase blog content unchanged for SEO

## Future Cleanup

Consider for future milestones:
- Delete unused page files (after confirming no SEO impact)
- Set up redirects for high-traffic Supabase URLs
- Create Axite-specific content for key pages

---

*Updated: 2026-01-23*
```
  </action>
  <verify>
Run: `test -f .planning/phases/09-navigation-cleanup/REMOVED-PAGES.md && echo "exists"`
Verify: File exists
Run: `grep "/auth" .planning/phases/09-navigation-cleanup/REMOVED-PAGES.md`
Verify: Auth page listed in removed pages
  </verify>
  <done>Removed pages documented for future reference</done>
</task>

</tasks>

<verification>
1. Run the dev server: `cd apps/www && pnpm dev`
2. Resize to mobile viewport (< 1024px)
3. Open hamburger menu
4. Verify Product accordion expands to show Axite features
5. Verify bottom buttons show "Contact sales" and "Get started"
6. Verify no Supabase-specific text visible
7. Check REMOVED-PAGES.md exists with comprehensive list
</verification>

<success_criteria>
- Mobile menu works with simplified nav structure
- Product accordion shows Axite features
- Mobile CTAs match desktop ("Contact sales", "Get started")
- No Supabase text in mobile menu (except logo which is deferred)
- REMOVED-PAGES.md documents all unlinked pages
</success_criteria>

<output>
After completion, create `.planning/phases/09-navigation-cleanup/09-04-SUMMARY.md`
</output>
