---
phase: 16-action-close
plan: 05
type: execute
wave: 2
depends_on: ["16-01", "16-02", "16-03", "16-04"]
files_modified:
  - apps/www/pages/index.tsx
autonomous: true

must_haves:
  truths:
    - "Homepage includes HowItWorksSection after PillarsSection"
    - "Homepage includes SecuritySection before FinalCTASection"
    - "Homepage includes FinalCTASection before Footer"
    - "Old Supabase sections are removed from homepage"
  artifacts:
    - path: "apps/www/pages/index.tsx"
      provides: "Updated homepage with all Phase 16 sections"
  key_links:
    - from: "apps/www/pages/index.tsx"
      to: "~/components/HowItWorksSection"
      via: "dynamic import"
      pattern: "HowItWorksSection"
    - from: "apps/www/pages/index.tsx"
      to: "~/components/SecuritySection"
      via: "dynamic import"
      pattern: "SecuritySection"
    - from: "apps/www/pages/index.tsx"
      to: "~/components/FinalCTASection"
      via: "dynamic import"
      pattern: "FinalCTASection"
---

<objective>
Integrate all Phase 16 sections into homepage and clean up remaining Supabase sections.

Purpose: Complete the landing page by adding new sections and removing irrelevant Supabase content.
Output: Updated homepage with proper section ordering and only Axite-relevant content.
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-action-close/16-CONTEXT.md

Current homepage:
@apps/www/pages/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update homepage with Phase 16 sections</name>
  <files>apps/www/pages/index.tsx</files>
  <action>
Update the homepage to include new sections and remove old Supabase sections.

1. Add dynamic imports for new sections:
   - const HowItWorksSection = dynamic(() => import('~/components/HowItWorksSection'))
   - const SecuritySection = dynamic(() => import('~/components/SecuritySection'))
   - const FinalCTASection = dynamic(() => import('~/components/FinalCTASection'))

2. Remove these Supabase-specific sections (imports and usage):
   - Products
   - HeroFrameworks
   - CustomerStories
   - BuiltWithSupabase
   - DashboardFeatures
   - TwitterSocialSection
   - OpenSourceSection
   - CTABanner (replaced by FinalCTASection)
   - getContent import (no longer needed)

3. Keep these sections:
   - Hero
   - ProofSection
   - TransformationSection
   - PillarsSection
   - Logos (social proof)

4. Final section order:
   ```
   <Layout>
     <Hero />
     <ProofSection />
     <Logos />
     <TransformationSection />
     <PillarsSection />
     <HowItWorksSection />
     <SecuritySection />
     <FinalCTASection />
   </Layout>
   ```

Note: Logos moved after ProofSection for better flow (trust indicators -> social proof -> value prop)

5. Clean up:
   - Remove unused getContent import and usage
   - Remove any props passed to removed components
  </action>
  <verify>
File contains: HowItWorksSection import and usage
File contains: SecuritySection import and usage
File contains: FinalCTASection import and usage
File does NOT contain: Products, HeroFrameworks, CustomerStories, BuiltWithSupabase
File does NOT contain: DashboardFeatures, TwitterSocialSection, OpenSourceSection
File does NOT contain: CTABanner (old CTA)
File does NOT contain: getContent
  </verify>
  <done>Homepage updated with Phase 16 sections, Supabase sections removed, proper section ordering</done>
</task>

<task type="auto">
  <name>Task 2: Verify build succeeds</name>
  <files></files>
  <action>
Run build to verify no broken imports or TypeScript errors:

```bash
cd apps/www && npm run build
```

If build fails:
- Check for missing imports
- Check for unused variable warnings
- Fix any TypeScript errors

Common issues:
- Forgetting to remove import statements for removed components
- Leaving props that reference removed data
  </action>
  <verify>
Build command exits with code 0
No TypeScript errors
No "module not found" errors
  </verify>
  <done>Build succeeds with all Phase 16 sections integrated</done>
</task>

</tasks>

<verification>
1. Homepage renders with correct section order
2. Build completes without errors
3. All Phase 16 components are imported and rendered
4. Supabase-specific sections are removed
5. No console errors on page load
</verification>

<success_criteria>
- Homepage has sections in order: Hero, ProofSection, Logos, TransformationSection, PillarsSection, HowItWorksSection, SecuritySection, FinalCTASection
- All Supabase-specific sections removed (Products, HeroFrameworks, CustomerStories, etc.)
- Build succeeds with no errors
- Page renders correctly
</success_criteria>

<output>
After completion, create `.planning/phases/16-action-close/16-05-SUMMARY.md`
</output>
