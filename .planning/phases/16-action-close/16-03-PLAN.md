---
phase: 16-action-close
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/www/components/FinalCTASection/index.tsx
autonomous: true

must_haves:
  truths:
    - "Final CTA section has outcome restatement headline"
    - "Primary 'Book Security Review' button is visible and prominent"
    - "No secondary CTA or View Docs link in this section"
  artifacts:
    - path: "apps/www/components/FinalCTASection/index.tsx"
      provides: "Final conversion section with outcome and CTA"
      exports: ["default"]
  key_links:
    - from: "apps/www/components/FinalCTASection/index.tsx"
      to: "/contact/sales"
      via: "Button Link href"
      pattern: "contact/sales"
---

<objective>
Create FinalCTASection component with outcome restatement and primary CTA button.

Purpose: Close the landing page with a clear call-to-action that restates the core value proposition.
Output: Standalone FinalCTASection component for bottom-of-page conversion.
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-action-close/16-CONTEXT.md

Reference patterns:
@apps/www/components/CTABanner/index.tsx - existing CTA banner pattern
@apps/www/components/Sections/EnterpriseCta.tsx - enterprise CTA pattern
@apps/www/components/Hero/Hero.tsx - for primary CTA button styling
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FinalCTASection component</name>
  <files>apps/www/components/FinalCTASection/index.tsx</files>
  <action>
Create a FinalCTASection component following the CTABanner pattern:

1. Structure:
   - Use similar layout to CTABanner (centered, generous padding)
   - Can use border-t for visual separation (optional)
   - Background: bg-alternative or similar subtle distinction

2. Content:
   - Outcome restatement headline (1 sentence):
     - Options: "Get complete visibility and control over every agent action"
     - Or: "Ship agents to production with confidence"
     - Or: "Enterprise-grade agent governance, starting today"
   - Subtext optional (if needed for context)

3. CTA Button:
   - Label: "Book Security Review" (consistent with hero)
   - Link: /contact/sales
   - Use Button component with size="medium" or size="large"
   - Primary style (not secondary/default)
   - NO secondary CTA (per user decision - no "View Docs" link)

4. Visual treatment:
   - Centered layout
   - Generous vertical padding (py-24 or py-32)
   - Clean, focused design
   - Heading gradient optional (like existing CTABanner)

Reference the CTABanner pattern but simplify:
- Single headline (not split)
- Single button (not two buttons)
- Keep the centered grid layout
  </action>
  <verify>
File exists: apps/www/components/FinalCTASection/index.tsx
Contains: "Book Security Review" text
Contains: Link to "/contact/sales"
Contains: Button component
Does NOT contain: "View Docs" or secondary button
  </verify>
  <done>FinalCTASection component created with outcome headline and single "Book Security Review" CTA button</done>
</task>

</tasks>

<verification>
1. `ls apps/www/components/FinalCTASection/index.tsx` shows file exists
2. Component has outcome restatement headline
3. Single "Book Security Review" button linking to /contact/sales
4. No secondary CTA or View Docs link
5. Follows CTABanner visual pattern
</verification>

<success_criteria>
- FinalCTASection component exists and exports correctly
- Outcome restatement headline present
- Primary "Book Security Review" CTA visible and links to /contact/sales
- NO secondary CTA (user decision)
- Follows Supabase CTABanner visual patterns
</success_criteria>

<output>
After completion, create `.planning/phases/16-action-close/16-03-SUMMARY.md`
</output>
