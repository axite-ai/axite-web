---
phase: 16-action-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/www/components/HowItWorksSection/index.tsx
autonomous: true

must_haves:
  truths:
    - "Three steps visible: Connect, Define, Monitor"
    - "Each step has number indicator, title, and description"
    - "Section follows Supabase design patterns"
  artifacts:
    - path: "apps/www/components/HowItWorksSection/index.tsx"
      provides: "How It Works three-step section"
      exports: ["default"]
  key_links:
    - from: "apps/www/components/HowItWorksSection/index.tsx"
      to: "~/components/Layouts/SectionContainer"
      via: "import"
      pattern: "SectionContainer"
---

<objective>
Create HowItWorksSection component with three-step workflow explanation.

Purpose: Guide users through the Connect -> Define -> Monitor workflow that explains how Axite works.
Output: Standalone HowItWorksSection component ready for homepage integration.
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-action-close/16-CONTEXT.md

Reference patterns:
@apps/www/components/Sections/HighlightColumns.tsx - for column layout pattern
@apps/www/components/TransformationSection/index.tsx - for section structure with SectionHeader
@apps/www/components/Layouts/SectionContainer.tsx - for container wrapper
@apps/www/components/UI/SectionHeader.tsx - for section header
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HowItWorksSection component</name>
  <files>apps/www/components/HowItWorksSection/index.tsx</files>
  <action>
Create a new HowItWorksSection component following these specifications:

1. Use SectionContainer and SectionHeader patterns from existing components
2. Three steps in a grid layout (single column mobile, 3 columns desktop):
   - Step 1: "Connect" - Connect your tools and MCP servers to Axite's gateway
   - Step 2: "Define" - Set policies for what agents can do and who can do it
   - Step 3: "Monitor" - Track every action with audit-grade logging

3. Each step card should include:
   - Step number indicator (e.g., "01", "02", "03") styled subtly
   - Title (Connect/Define/Monitor)
   - Brief description (1-2 sentences)

4. Visual treatment:
   - Use Panel component for cards (like TransformationSection)
   - Subtle background for each step card
   - Clean typography following existing patterns

5. NO CTA after the steps (per user decision in CONTEXT.md)

6. Section header:
   - subtitle: "Getting started"
   - title: "Three steps to governance"
   - Optional paragraph if needed for context

Follow the HighlightColumns pattern for the grid layout, using:
- grid grid-cols-1 lg:grid-cols-3 gap-6 or gap-8
  </action>
  <verify>
File exists: apps/www/components/HowItWorksSection/index.tsx
Contains: SectionContainer, SectionHeader imports
Contains: Three step definitions (Connect, Define, Monitor)
Contains: grid-cols-1 lg:grid-cols-3
No CTA button in the component
  </verify>
  <done>HowItWorksSection component created with 3 steps (Connect, Define, Monitor), uses Supabase patterns, no CTA</done>
</task>

</tasks>

<verification>
1. `ls apps/www/components/HowItWorksSection/index.tsx` shows file exists
2. File exports default component
3. File uses SectionContainer and SectionHeader patterns
4. Three steps are defined with proper content
5. No Button or CTA element in component
</verification>

<success_criteria>
- HowItWorksSection component exists and exports correctly
- Component follows Supabase design patterns (SectionContainer, SectionHeader, Panel)
- Three steps visible with appropriate content
- Responsive grid layout (1 col mobile, 3 cols desktop)
- No CTA in this section (user decision)
</success_criteria>

<output>
After completion, create `.planning/phases/16-action-close/16-01-SUMMARY.md`
</output>
